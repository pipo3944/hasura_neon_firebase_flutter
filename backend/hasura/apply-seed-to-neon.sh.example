#!/bin/bash

set -e

# Neon development branch の Direct connection 文字列を設定してください
# 取得方法: Neon Console > development branch > Dashboard > Connection Details > Direct connection
DATABASE_URL='postgresql://neondb_owner:YOUR_PASSWORD@YOUR_ENDPOINT.ap-southeast-1.aws.neon.tech/neondb?sslmode=require'

SEEDS_DIR="/Users/YOUR_USERNAME/Desktop/dev/hasura_flutter/backend/hasura/seeds"

echo "Applying seed data to Neon development branch..."

docker run --rm \
  -v "$SEEDS_DIR:/seeds" \
  postgres:15 \
  psql "$DATABASE_URL" \
  -f /seeds/default/1_test_data.sql

echo ""
echo "🎉 Seed data applied successfully!"
